exports.routes = {
    root: 'pokemonshowdown.com',
    client: 'play.pokemonshowdown.com',
    replays: 'replay.pokemonshowdown.com',
    // Sibling sites linked from the header nav. Both are optional: `build` derives them from `client` by
    // swapping its first label, which is right whenever the sites are `<service>.<domain>`.
    dex: 'dex.pokemonshowdown.com',
    analysis: 'analysis.pokemonshowdown.com',
};
